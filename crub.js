function creat(){
    let btn = document.createElement('button')
    btn.innerHTML = "question";
document.body.appendChild(btn);
}
//line two creats a button element
function read(){
    let btn = document.readyState('button')
    btn.innerHTML = "upRead";
}